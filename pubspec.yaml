name: pawpoints
description: Eine Belohnungs-App für Doggy & Herrchen.
publish_to: 'none' # Dies ist ein privates Projekt

version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2


  # Standard Icons für iOS-Designs
  cupertino_icons: ^1.0.6

  # Lokale Speicherung
  shared_preferences: ^2.2.2

  # Bilder auswählen
  image_picker: ^1.0.4

  # Dateioperationen
  path_provider: ^2.1.1
  path: ^1.8.3

  # Lokalisierung (für deutsche Sprache etc.)
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png

