// lib/Settings/herrchen_shop_screen.dart
import 'package:flutter/material.dart';

class HerrchenShopScreen extends StatelessWidget {
  const HerrchenShopScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Shop wird bald verfügbar sein')),
    );
  }
}
